var PassThrough = require('readable-stream').PassThrough;
var inherits = require('inherits');

var ImageStream = function ImageStream(opts) {
  if (!(this instanceof ImageStream)) return new ImageStream(opts);

  PassThrough.call(this);

  for (var key in opts) {
    this[key] = opts[key];
  }
};

inherits(ImageStream, PassThrough);

module.exports = ImageStream;

